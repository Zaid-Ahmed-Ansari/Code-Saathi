import React from 'react';

import BackgroundPaths from './ui/background-paths.jsx';

const HeroSection = () => (
  <section className="min-h-screen bg-gradient-to-br from-gray-900 via-indigo-900 to-purple-900 flex items-center justify-center relative overflow-hidden">
    <BackgroundPaths />
  </section>
);

export default HeroSection; 